use process
use io

bind todo "println!(\"Not implemented yet\"); std::process::exit(1);"

macro assert {
    let is_pass, msg {
        is_pass ! if {
            msg println
            1 exit
        }
    }
}